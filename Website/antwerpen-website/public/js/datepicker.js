jQuery(document).ready(function($){

	$('.input-group.date').datepicker({
    format: "yyyy-mm-dd",
    weekStart: 1,
    todayBtn: "linked",
    language: "nl-BE",
    autoclose: true,
    todayHighlight: true
});

});
